name: 'BTC EmbeddedPlatform GitHub Actions Library- Startup'
description: 'Starts up BTC EmbeddedPlatform'
inputs:
  profilePath:
    description: 'Path of the profile. If it does not exist, it will be created. The path can be absolute or relative to the jenkins jobs workspace'
    required: true
runs:
  using: 'node16'
  main: '/lib/globalEntrypoint.js'